/**
 * Roughly, for the download prompt. The archive is bzip2 so the disk cost is
 * higher. Shared because the renderer quotes the size before the download
 * starts, when main has no status to report yet.
 */
export const MODEL_DOWNLOAD_MB = 680
