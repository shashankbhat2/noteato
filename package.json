{
  "name": "noteato",
  "version": "0.7.0",
  "description": "A minimal, bloat-free, block-based note taking app for Mac (Apple Silicon) — markdown, dictation, sticky notes. No accounts, no cloud, no lock-in.",
  "main": "./out/main/index.js",
  "author": "Shashank Bhat",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/shashankbhat2/noteato.git"
  },
  "homepage": "https://github.com/shashankbhat2/noteato",
  "bugs": "https://github.com/shashankbhat2/noteato/issues",
  "private": true,
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "clean": "rm -rf dist out",
    "build:mac": "npm run clean && electron-vite build && electron-builder --mac",
    "release": "npm run clean && electron-vite build && electron-builder --mac --publish always",
    "preview": "electron-vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.node.json && tsc --noEmit -p tsconfig.web.json"
  },
  "dependencies": {
    "@electron-toolkit/preload": "^3.0.1",
    "@electron-toolkit/utils": "^3.0.0"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.110.0",
    "@blocknote/core": "^0.51.4",
    "@blocknote/mantine": "^0.51.4",
    "@blocknote/react": "^0.51.4",
    "@tabler/icons-react": "^3.44.0",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^4.3.4",
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "electron-vite": "^5.0.0",
    "openai": "^6.46.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
